module item.plant.functions.BirthLocationFunctions;

import item.plant.Plant;
import world.World;

/**
 * Gets the nearest land location for the plant to place itself
 */
Coordinate getNearestLandLocation(Plant forWhom){
    return nullCoord;
}

/**
 * Gets the nearest water location fro the plant to place itself
 */
Coordinate getNearestWaterLocation(Plant forWhom){
    return nullCoord;
}
